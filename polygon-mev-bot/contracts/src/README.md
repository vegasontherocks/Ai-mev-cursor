# Smart Contracts

## 🔮 Generated by Thirdweb Nebula

The contracts in the `generated/` folder are created by **Thirdweb Nebula** (blockchain-trained LLM).

### Generate Contracts

```bash
cd /workspace/polygon-mev-bot
./GENERATE_NOW.sh
```

This will create:
- `generated/MEVExecutor.sol` - Main contract with ALL logic
- `generated/DEXAdapter.sol` - Multi-DEX interactions
- `generated/AaveAdapter.sol` - Aave V3 liquidations
- `generated/JITAdapter.sol` - Uniswap V3 JIT liquidity
- `generated/OracleLib.sol` - Chainlink TWAP validation
- `generated/Interfaces.sol` - All protocol interfaces

### Why Nebula?

Nebula is trained on 1B+ blockchain transactions and knows:
- Exact flash loan patterns
- Gas optimization techniques
- Security best practices
- DeFi protocol mechanics

**It generates better MEV contracts than any human!**

### Compile & Deploy

```bash
# After generation
forge build

# Should compile without errors!

# Test
forge test -vv

# Deploy
forge script script/Deploy.s.sol --broadcast
```

---

**Let the blockchain AI write your blockchain code!** 🔮⛓️
